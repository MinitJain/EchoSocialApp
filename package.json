{
  "name": "echo",
  "version": "1.0.0",
  "private": true,
  "description": "Echo — a full-stack AI-enabled microblogging platform built with the MERN stack, featuring secure authentication, modern UI/UX, dark mode, and an integrated AI writing assistant.",
  "scripts": {
    "dev": "concurrently \"cd backend && npm run dev\" \"cd frontend && npm run dev\"",
    "build": "cd frontend && npm run build",
    "install-all": "cd backend && npm install && cd ../frontend && npm install",
    "start": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
